{application, eiko, [
        {description, "IRC Bot"},
        {vsn, "0.1"},
        {modules, [
                irc_sup,
                irc_network_sup,
                irc_config,
                irc_network,
                irc_lib
            ]},
        {registered, []},
        {applications, [kernel, stdlib]},
        {mod, {eiko_app, [
                    {config, 'config/settings.cfg'}
                ]}},
        {start_phases, []}
    ]}.
